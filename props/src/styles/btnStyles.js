import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    btn: {
        borderRadius: 12,
        padding: 10,
        marginTop: 20,
        width: 180,
        height: 52,
        alignItems: 'center',
        justifyContent: 'center',
    },
})